name: courierwarehousing
description: "Courier & Warehousing Management Platform"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  fl_chart: ^0.69.0
  intl: ^0.20.1
  uuid: ^4.5.1
  shared_preferences: ^2.3.3
  google_fonts: ^6.2.1
  http: ^1.2.0
  supabase_flutter: ^2.8.0
  pdf: ^3.11.1
  printing: ^5.13.4
  csv: ^6.0.0
  barcode: ^2.2.9
  video_player: ^2.9.2
  mobile_scanner: ^6.0.2
  image_picker: ^1.1.2
  connectivity_plus: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/images/one_village_logo.png
    - assets/images/hero_port.jpg
    - assets/images/sea_freight.jpg
    - assets/images/air_freight.jpg
    - assets/images/warehousing.jpg
    - assets/images/door_to_door.jpg
    - assets/images/customs_clearance.jpg
    - assets/images/forklift_warehouse.jpg
    - assets/videos/hero_background.mp4

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
